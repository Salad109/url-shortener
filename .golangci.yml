version: "2"

linters:
  default: standard

formatters:
  enable:
    - gofumpt
    - goimports
    - gci
  settings:
    gci:
      custom-order: true
      sections:
        - standard
        - prefix(github.com/Salad109/url-shortener)
        - default
